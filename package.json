{
  "name": "erpeg",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "map": "node scripts/build-map.mjs",
    "dev": "node scripts/build-map.mjs && vite --host",
    "build": "node scripts/build-map.mjs && tsc --noEmit && vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "phaser": "^4.2.1"
  },
  "devDependencies": {
    "clipper-lib": "^6.4.2",
    "typescript": "^7.0.2",
    "vite": "^8.3.1"
  }
}
