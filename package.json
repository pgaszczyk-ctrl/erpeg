{
  "name": "erpeg",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "map": "node scripts/build-maps.mjs",
    "dev": "node scripts/build-maps.mjs && vite --host",
    "build": "node scripts/build-maps.mjs && tsc --noEmit && vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "phaser": "^4.2.1",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.6",
    "clipper-lib": "^6.4.2",
    "typescript": "^7.0.2",
    "vite": "^8.3.1"
  }
}
